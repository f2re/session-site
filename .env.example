# =============================================================================
# BG Remove Website - Environment Variables Template
# =============================================================================
#
# Этот файл используется telegram-bots-platform для создания .env
# Не удаляйте и не изменяйте формат!
#
# =============================================================================

# -----------------------------------------------------------------------------
# NODE.JS SETTINGS
# -----------------------------------------------------------------------------
NODE_ENV=production

# Локальный порт для Docker контейнера (доступен только через localhost)
# ВАЖНО: В docker-compose.yml настроено 127.0.0.1:${PORT}:3000 для безопасности
# Это означает, что контейнер доступен только локально, а nginx проксирует запросы
# Nginx должен быть настроен на proxy_pass http://localhost:${PORT}
PORT=3000

# -----------------------------------------------------------------------------
# BOT CONFIGURATION
# -----------------------------------------------------------------------------
# Telegram Bot Username (without @)
BOT_USERNAME=your_bot_username

# -----------------------------------------------------------------------------
# YANDEX METRIKA
# -----------------------------------------------------------------------------
# Yandex Metrika Counter ID (например: 12345678)
# Получить ID: https://metrika.yandex.ru/ -> Создать счетчик
#
# ВАЖНО:
# - Если не указать, Yandex Metrika будет отключена
# - Загружается в RUNTIME (не требует пересборки Docker образа)
# - Идеально для telegram-bots-platform - просто укажите в .env
# - Формат: только цифры, без кавычек
NEXT_PUBLIC_YANDEX_METRIKA_ID=

# -----------------------------------------------------------------------------
# DATABASE (auto-configured by platform)
# -----------------------------------------------------------------------------
DB_HOST=postgres
DB_PORT=5432
DB_NAME=bg_site_db
DB_USER=bg_site_user
DB_PASSWORD=

# -----------------------------------------------------------------------------
# PRICING CONFIGURATION
# -----------------------------------------------------------------------------
# Package 1 - Trial
PACKAGE_1_NAME=Пробный
PACKAGE_1_IMAGES=5
PACKAGE_1_PRICE=99

# Package 2 - Starter
PACKAGE_2_NAME=Стартовый
PACKAGE_2_IMAGES=15
PACKAGE_2_PRICE=199

# Package 3 - Pro (Most Popular)
PACKAGE_3_NAME=Профи
PACKAGE_3_IMAGES=50
PACKAGE_3_PRICE=699

# Package 4 - Business
PACKAGE_4_NAME=Бизнес
PACKAGE_4_IMAGES=150
PACKAGE_4_PRICE=1499

# Free images for new users
FREE_IMAGES_COUNT=3

# -----------------------------------------------------------------------------
# REFERRAL PROGRAM
# -----------------------------------------------------------------------------
# Reward given to referrer when new user clicks /start (in images)
REFERRAL_REWARD_START=5

# -----------------------------------------------------------------------------
# METRIKA GOALS
# -----------------------------------------------------------------------------
METRIKA_GOAL_START=start_bot
METRIKA_GOAL_FIRST_IMAGE=first_image
METRIKA_GOAL_PURCHASE=purchase

# -----------------------------------------------------------------------------
# ВНИМАНИЕ!
# -----------------------------------------------------------------------------
# После создания бота через add-bot.sh:
# 1. Отредактируйте .env файл в /opt/telegram-bots-platform/bots/bg-site/.env
# 2. Укажите правильный BOT_USERNAME
# 3. Добавьте NEXT_PUBLIC_YANDEX_METRIKA_ID (если нужен)
# 4. Перезапустите: cd /opt/telegram-bots-platform/bots/bg-site && docker compose restart
